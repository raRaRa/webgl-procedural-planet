<configuration>

    <system.webServer>

        <staticContent>
            <mimeMap fileExtension=".fragment" mimeType="text/plain" />
            <mimeMap fileExtension=".vertex" mimeType="text/plain" />
     </staticContent>

    </system.webServer>

</configuration> 